export const metadata = {
  title: 'Regex Tester Online - Test Regular Expressions with Highlighting | TextTools Pro',
  description: 'Test regular expressions with real-time matching, syntax highlighting, capture groups, and find-replace. Common regex patterns included. Premium tool by TextTools Pro.',
  keywords: 'regex tester, regular expression tester, regex online, regex checker, test regex, regex pattern matcher, regex debugger',
};

export default function Layout({ children }) {
  return children;
}
