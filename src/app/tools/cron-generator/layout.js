export const metadata = {
  title: 'Cron Expression Generator - Build Cron Jobs Visually',
  description: 'Build and understand cron expressions visually. See human-readable descriptions, next run times, and use quick presets. Premium tool by TextTools Pro.',
  keywords: 'cron expression generator, cron job builder, crontab generator, cron schedule, cron expression builder, cron parser',
};

export default function Layout({ children }) {
  return children;
}
