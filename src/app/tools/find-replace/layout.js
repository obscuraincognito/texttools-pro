export const metadata = {
  title: 'Free Find and Replace Text Online - Regex Support',
  description: 'Find and replace text online with regex support. Remove blank lines, duplicate lines, and trim whitespace. Free tool, no sign-up, runs in your browser.',
  keywords: 'find and replace online, text find replace, regex find replace, remove duplicate lines, remove blank lines',
};

export default function Layout({ children }) {
  return children;
}
