export const metadata = {
  title: 'Free HTML Entity Encoder Decoder Online - Escape HTML | TextTools Pro',
  description: 'Convert special characters to HTML entities or decode HTML entities back to text. Escape HTML for safe display. Free online tool, no sign-up required.',
  keywords: 'HTML entity encoder, HTML entity decoder, escape HTML, HTML entities, HTML special characters, unescape HTML',
};

export default function Layout({ children }) {
  return children;
}
