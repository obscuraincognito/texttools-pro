export const metadata = {
  title: 'JSON to YAML Converter Online - Convert JSON & YAML',
  description: 'Convert between JSON and YAML formats online. Handles nested objects, arrays, and special characters with customizable indentation. Premium tool by TextTools Pro.',
  keywords: 'JSON to YAML, YAML to JSON, JSON YAML converter, convert JSON online, YAML converter, JSON YAML online',
};

export default function Layout({ children }) {
  return children;
}
