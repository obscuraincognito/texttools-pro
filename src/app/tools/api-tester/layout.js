export const metadata = {
  title: 'API Tester - Build & Send HTTP Requests Online',
  description: 'Test REST APIs directly in your browser. Build GET, POST, PUT, DELETE, and PATCH requests with custom headers and body. View response status, timing, headers, and formatted JSON. Premium tool by TextTools Pro.',
  keywords: 'API tester, HTTP request tester, REST API client, send GET POST request online, API debugging tool, HTTP client online, test API endpoint',
};

export default function Layout({ children }) {
  return children;
}
