export const metadata = {
  title: 'UUID Generator Online - Generate Random UUID v4',
  description: 'Generate random UUID v4 identifiers online. Create single or bulk UUIDs with formatting options. Uppercase, no dashes, and copy all. Free tool by TextTools Pro.',
  keywords: 'UUID generator, UUID v4, random UUID, GUID generator, unique identifier, generate UUID online',
};

export default function Layout({ children }) {
  return children;
}
