export const metadata = {
  title: 'JSON Formatter & Validator Online - Format, Minify JSON',
  description: 'Format, validate, and minify JSON online. Beautify JSON with 2 or 4 space indentation. Check JSON syntax errors instantly. Premium tool by TextTools Pro.',
  keywords: 'JSON formatter, JSON validator, JSON beautifier, JSON minifier, format JSON online, pretty print JSON, JSON checker',
};

export default function Layout({ children }) {
  return children;
}
