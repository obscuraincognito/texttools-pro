export const metadata = {
  title: 'Free Base64 Encoder Decoder Online - Encode & Decode Base64 | TextTools Pro',
  description: 'Encode text to Base64 or decode Base64 strings back to plain text instantly. Free online tool, no sign-up required, runs entirely in your browser.',
  keywords: 'base64 encoder, base64 decoder, base64 encode online, base64 decode online, base64 converter',
};

export default function Layout({ children }) {
  return children;
}
