export const metadata = {
  title: 'JWT Decoder Online - Decode & Inspect JSON Web Tokens',
  description: 'Decode and inspect JSON Web Tokens online. View header, payload claims, expiration status, and raw JSON. Premium tool by TextTools Pro.',
  keywords: 'JWT decoder, JSON Web Token decoder, JWT inspector, decode JWT, JWT parser, JWT claims viewer',
};

export default function Layout({ children }) {
  return children;
}
