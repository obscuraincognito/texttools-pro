export const metadata = {
  title: 'Markdown Editor Online - Live Preview & HTML Export | TextTools Pro',
  description: 'Write Markdown with a live side-by-side preview. Export to HTML with one click. Supports headers, lists, code blocks, and more. Premium tool by TextTools Pro.',
  keywords: 'markdown editor, markdown preview, markdown to HTML, online markdown editor, markdown converter, live markdown preview',
};

export default function Layout({ children }) {
  return children;
}
